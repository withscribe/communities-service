type Community {
  id: ID! @unique
  ownerId: ID
  moderatorId: ID
  name: String!
  description: String
  createdAt: DateTime!
  updatedAt: DateTime!
  membersIds: [ID!]!
  bannedMembersIds: [ID!]!
  privacy: Privacy @default(value: PUBLIC)
}

enum Privacy {
  PUBLIC
  PRIVATE
  INVITE_ONLY
}

